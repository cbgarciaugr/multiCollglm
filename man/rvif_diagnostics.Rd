% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rvif_diagnostics.R
\name{rvif_diagnostics}
\alias{rvif_diagnostics}
\alias{rvif_diagnostics.glmnet}
\alias{rvif_diagnostics.cv.glmnet}
\title{Redefined Variance Inflation Factor (RVIF) diagnostics}
\usage{
rvif_diagnostics(model, ...)

\method{rvif_diagnostics}{glmnet}(
  model,
  x,
  y,
  family,
  s,
  weights = NULL,
  offset = NULL,
  center = FALSE,
  scale = "unit",
  tol = 1e-30,
  ...
)

\method{rvif_diagnostics}{cv.glmnet}(
  model,
  x,
  y,
  family,
  s = "lambda.min",
  weights = NULL,
  offset = NULL,
  center = FALSE,
  scale = "unit",
  tol = 1e-30,
  ...
)
}
\arguments{
\item{model}{A \code{glm}, \code{glmnet} or \code{cv.glmnet} object.}

\item{...}{Additional arguments passed to the methods (see \code{x}, \code{y},
\code{family}, \code{s}, \code{weights}, \code{offset} for \code{glmnet}/\code{cv.glmnet}).}

\item{x}{Predictor matrix (the same one used to fit the \code{glmnet}
model), with \code{colnames}.}

\item{y}{Response variable used to fit the \code{glmnet} model.}

\item{family}{GLM family (a \code{family} object, e.g.
\code{Gamma(link = "inverse")}) used to refit a \code{glm} on the active set of
variables at the given lambda.}

\item{s}{Lambda value (numeric) for a \code{glmnet} object. For \code{cv.glmnet}
it can also be \code{"lambda.min"} or \code{"lambda.1se"}.}

\item{weights}{Optional prior weights for the refit.}

\item{offset}{Optional offset for the refit.}

\item{center}{Either \code{FALSE} (default; no centering, as originally
proposed and required to detect non-essential collinearity involving
the intercept) or any value accepted by \code{\link[=scale]{scale()}}'s own \code{center}
argument: \code{TRUE} to center each column on its mean, or a numeric
vector of per-column values to subtract. For a canonical-link GLM
with an intercept, centering the IRLS-weighted design matrix is
mathematically guaranteed to drop its rank by exactly one (since
\code{sqrt(w) * eta} is then constant), which \code{\link[=bkw_diagnostics]{bkw_diagnostics()}} and
\code{\link[=rvif_diagnostics]{rvif_diagnostics()}} report as an error rather than silent \code{NaN}s;
this is the classical reason Belsley, Kuh and Welsch advise against
centering for collinearity diagnostics.}

\item{scale}{Either \code{"unit"} (default; each column is scaled to
Euclidean/L2 unit length, as originally proposed and required for the
classical condition-index thresholds and the RVIF formula) or any
value accepted by \code{\link[=scale]{scale()}}'s own \code{scale} argument: \code{TRUE} for
root-mean-square scaling, \code{FALSE} for no scaling, or a numeric vector
of per-column divisors.}

\item{tol}{Tolerance used by \code{\link[rvif:rvifs]{rvif::rvifs()}} to decide whether the
system is computationally singular (default \code{1e-30}).}
}
\value{
An object of class \code{multicollglm_rvif} wrapping the data frame
returned by \code{\link[rvif:rvifs]{rvif::rvifs()}} (\code{table}, with columns \code{RVIF} and \verb{\%},
one row per column of the design matrix, named after the model's
variables).
}
\description{
Computes the Redefined Variance Inflation Factor (RVIF) of Salmeron,
Garcia and Garcia (2025), via \code{\link[rvif:rvifs]{rvif::rvifs()}}, on the same design
matrix used by \code{\link[=condition_number]{condition_number()}} and \code{\link[=bkw_diagnostics]{bkw_diagnostics()}}: weighted
by the IRLS weights at convergence and, by default, scaled to unit
length by column (without centering); see \code{center}/\code{scale} below.
\code{\link[rvif:rvifs]{rvif::rvifs()}} is always called with \code{ul = TRUE}, so it re-scales
that matrix to Euclidean unit length right before computing the RVIF,
whatever \code{center}/\code{scale} were used to build it (a matrix that is
already unit length, the default, is left unchanged by this step).
Unlike the classical VIF, the RVIF is able to
detect both essential collinearity (near-linear relationships among
the regressors) and non-essential collinearity (near-linear
relationships between the intercept and one or more regressors), and
also decomposes, for each variable, the percentage of near
collinearity it is responsible for.
}
\examples{
set.seed(1)
n <- 200
x1 <- rnorm(n); x2 <- x1 + rnorm(n, sd = 0.05); x3 <- rnorm(n); x4 <- rnorm(n)
mu <- exp(1 + 0.3 * x1 + 0.3 * x2 - 0.2 * x3 + 0.1 * x4)
y <- rgamma(n, shape = 5, rate = 5 / mu)
mod <- glm(y ~ x1 + x2 + x3 + x4, family = Gamma(link = "inverse"))
rvif_diagnostics(mod)

}
\references{
Salmeron, R., Garcia, C.B. and Garcia, J. (2025). A redefined Variance
Inflation Factor: overcoming the limitations of the Variance Inflation
Factor. Computational Economics, 65, 337-363.
\doi{10.1007/s10614-024-10575-8}
}
